# See if we can cast the automake spell. -*-Makefile-*-
#

#
AUTOMAKE_OPTIONS = no-dependencies 1.4

SUBDIRS = add add_fmt cache cmp conv del export parse # sub # netstr=add/parse iovec=read/write split loc(ex_test1.c)
