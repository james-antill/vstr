# See if we can cast the automake spell. -*-Makefile-*-
#

#
AUTOMAKE_OPTIONS = no-dependencies 1.4

SUBDIRS = cache cmp conv fmt parse # netstr add sub mov cstr iovec=read/write split loc(ex_test1.c)
